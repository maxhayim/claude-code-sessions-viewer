---
description: List every Claude Code session ever recorded on this machine, grouped by project, most recent first
argument-hint: ""
---

Run this command and show me the full output exactly as printed, formatted as a readable list grouped by project:

```
python3 "${CLAUDE_PLUGIN_ROOT}/scripts/list_sessions.py"
```

After showing the output, briefly note which project has the most recent activity and ask if I want to `/resume` a specific session.
